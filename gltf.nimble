# Package

version     = "0.1.0"
author      = "copygirl"
description = "Library for loading scenes and models in the GL Transmission Format (glTF)"
license     = "MIT"

srcDir   = "src"
skipDirs = @["tests", "examples"]

# Dependencies

requires "nim >= 0.18.0"
requires "glm >= 1.1.1"
